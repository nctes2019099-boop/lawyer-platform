# تقرير فحص ومعالجة منصة المحاماة (ميزان العدالة)

تم إجراء فحص شامل للمشروع (الميزات، الأمان، جودة الكود، والاعتماديات)
ومعالجة السلبيات الحرجة. هذا التقرير يلخص ما وُجد وما تم إصلاحه.

## ✨ الميزات الإيجابية الموجودة

- معمارية Next.js 16 + React 19 + Prisma + SQLite واضحة ومنظّمة.
- دعم كامل للغة العربية واتجاه RTL مع نظام ألوان ذهبي/كحلي مميز.
- فصل منطقي جيد بين الشاشات (screens) والمكوّنات وواجهات برمجة التطبيقات.
- استخدام JWT عبر `jose` مع كوكي `httpOnly`.
- تحديد المعدل (rate limiting) على مستوى المستخدم في عدد من المسارات.
- واجهة جوال (PWA + Capacitor) ووضع ليلي/نهاري وإمكانية وصول (skip link, reduced motion).
- اختبارات وحدة أساسية + تصدير CSV + مخطط اشتراكات/مدفوعات.

## 🔒 السلبيات الأمنية التي تم إصلاحها

1. **تجاوز المصادقة عبر ترويسة `x-user-id`**
   - كان الكود يقبل هوية المستخدم من كوكي/ترويسة يتحكم بها العميل → استيلاء على حسابات.
   - تم الإزالة بالكامل من `session.ts` و`api.ts`.

2. **تسجيل دخول تلقائي مفعل افتراضياً (`DEV_AUTO_LOGIN`)**
   - كان يعمل حتى في الإنتاج. أُجبر على التعطيل في الإنتاج، وأصبح `false` افتراضياً.

3. **سرّ جلسة افتراضي ضعيف**
   - تم فرض `NEXTAUTH_SECRET` قوي (≥16 حرف) في الإنتاج مع فشل سريع واضح.

4. **كوكي الجلسة غير آمن**
   - `secure` كان `false` دائماً؛ صار مفعّلاً في الإنتاج.

5. **إعادة تعيين كلمة المرور معطّلة**
   - كان المسار يتحقق من `token` ويتجاهله تماماً ولا يحدّث كلمة المرور.
   - تمت إضافة نموذج `PasswordResetToken` وتوقيع JWT للرمز والتحقق منه وتعليمه كمستخدم.

6. **تعداد المستخدمين (Enumeration)**
   - `forgot-password` كان يكشف وجود البريد. أصبحت الاستجابة موحّدة.

7. **تجاوز كتلة (Mass Assignment)** في `PATCH`
   - كان `cases/clients/appointments/notes/profile` يمرّر الجسم مباشرة إلى Prisma مما
     يسمح بتعديل `ownerId`/`isAdmin`/الخ. تم استخدام قوائم بيضاء (whitelist) وحقول محددة.

8. **ثغرة رفع الصلاحيات في الملف الشخصي**
   - تم حماية `PATCH /user/profile` بحقول محددة وتحقق Zod.

9. **IDOR عند إنشاء الموارد المرتبطة**
   - كان يمكن ربط قضية/موكل/مستند يخص مستخدمًا آخر.
   - تمت إضافة `userOwnsResources()` وتطبيقه في:
     `cases, clients, appointments, tasks, time-entries, documents, petitions, transactions`.

10. **التحقق من الدفع (`/payments/verify`)**
    - كان أي مستخدم يؤكد دفعته بنفسه. الآن: القبول فقط لمزود `mock` خارج الإنتاج،
      والتحقق من السعر الحالي للخطة، ومنع التأكيد اليدوي في الإنتاج (الدفع الحقيقي عبر الـ webhook).

11. **حقن صيغ CSV (CSV Injection)**
    - تتم تحييد الخلايا التي تبدأ بـ `= + - @` وإضافة BOM للعربية.

12. **غياب ترويسات الأمان**
    - تمت إضافتها في `src/proxy.ts`: `X-Content-Type-Options`, `X-Frame-Options`,
      `Referrer-Policy`, `Permissions-Policy`, و`Strict-Transport-Security` في الإنتاج.

13. **لا يوجد حد لعناصر `limit` في الاستعلامات** → تمرير سهل إلى DoS.
    - تمت إضافة `getPagination()` مع حدّ أقصى وتطبيقه في كل قوائم الـ API.

14. **تسرّب الذاكرة في معدّل التحديد**
    - خريطة الطلبات كانت تنمو للأبد. تمت إضافة كنس دوري (sweep) للمفاتيح القديمة.

15. **تسجيل الدخول والتسجيل بلا تحديد معدّل على مستوى IP**
    - تمت إضافة `rateLimitIP` + استخراج IP، ورفع الحد الأدنى لكلمة المرور إلى 8 أحرف.

16. **نموذج إنشاء خطط الاشتراك للمشرف يمرّر جسمًا خامًا** → تم التحقق عبر Zod.

17. **ثغرة في نقطة نهاية `laws` PATCH** (نوع بيانات غير متحقق منه) → تم التحقق.

## 🧰 جودة الكود والبنية التي تم تحسينها

- إصلاح أخطاء TypeScript في `export/route.ts`.
- استبدال خطوط Google (فشل البناء دون إنترنت) بمكدّس نظام مع دعم العربية.
- ترقية `next` من `16.0.0` (ثغرة CVE-2025-66478) إلى `16.3.2`.
- إضافة إعداد ESLint v9 flat config (كان `next lint` معطّلاً في Next 16).
- تحويل `InputProps`/`TextareaProps` إلى أنواع بدل واجهات فارغة.
- توحيد استجابات الأخطاء وإضافة تحقق Zod في معظم مسارات الكتابة.
- تحسين احتساب المدة في `time-entries` وحفظ `completedAt` بشكل صحيح.
- إضافة اختبارات أمان (CSV injection + bounds of pagination).

## ⚙️ ملاحظات بيئية (Sandbox)

- لا تتوفّر اتصالية لتحميل محرّك Prisma الثنائي في بيئة الفحص. يعمل البناء
  (`npm run build`) واختبارات الوحدة (`npm run test:run`) بنجاح.
- للتشغيل الكامل محلياً (مع قاعدة بيانات):
  ```bash
  npm install
  npx prisma generate
  npx prisma db push
  npm run db:seed
  npm run dev
  ```

## ✅ نتائج التحقق

- `npx tsc --noEmit` — ناجح.
- `npx eslint "src/**/*.{ts,tsx}"` — صفر أخطاء (تحذيرات فقط).
- `npx vitest run` — 9 اختبارات ناجحة.
- `npm run build` — ناجح.

## 🔭 توصيات مستقبلية (غير حرجة)

- نقل معدّل التحديد والجلسات إلى مخزن مشترك (Redis) عند تعدد العُقد/Serverless.
- استخدام آلية بريد حقيقية لإرسال روابط إعادة التعيين بدل طباعتها في الطرفية.
- التحقق من الـ webhooks (توقيع + قائمة IP) عند تفعيل بوابات الدفع الحقيقية.
- اختبارات تكامل لمسارات الـ API (مصادقة/تجاوز/ملكية) باستخدام قاعدة اختبار.
- تفعيل سجلات التدقيق (AuditLog) فعلياً عند العمليات الحساسة.

## 📱 تطوير الموبايل والميزات الجديدة (المرحلة الثانية)

### ميزات أُضيفت
- **جلسات المحكمة (Sessions)** — نموذج `Session` مع مسارات `GET/POST/PATCH/DELETE`
  مرتبطة بالقضية وملكيتها محميّة.
- **التذكيرات (Reminders)** — مواعيد نهائية/جلسات/مدفوعات مع مسارات كاملة.
- **لوحة بيانات موحّدة** `/api/dashboard` تُرجع كل الإحصائيات في طلب واحد
  (مهام متأخرة، جلسات قادمة، التذكيرات، المالية الشهرية).
- **تقويم موحّد** `/api/calendar?month=YYYY-MM` يجمع المواعيد والجلسات
  والمهام والتذكيرات مجمّعة حسب اليوم.
- **إضافة/تعديل القوانين** (مسار `POST /api/laws`).
- **روابط وثائق آمنة** (نموذج `DocumentAccess` لمشاركة محددة بوقت).

### الموبايل (PWA + Capacitor)
- توليد أيقونات PNG كاملة (72→512) وأيقونة maskable وApple touch وsplash
  وصورة Open Graph من شعار ذهبي-كحلي.
- `manifest.json` موسّع (أيقونات متعددة، اختصارات، لغة/اتجاه، فئات).
- Service Worker (`public/sw.js`) للعمل دون اتصال + مكوّن تسجيل PWA.
- إعداد `capacitor.config.ts` للإنتاج مع المكوّنات الإضافية
  (SplashScreen, StatusBar, Keyboard).
- بناء ثابت للموبايل عبر `scripts/build-mobile.mjs` (يستبعد مسارات الـ API
  مؤقتاً لأنها تتطلب خادماً) ومسارات npm: `build:mobile`, `cap:sync`,
  `apk:debug`, `apk:release`.
- دليل بناء شامل على ويندوز في `MOBILE.md`.
- `NEXT_PUBLIC_API_URL` لربط التطبيق الثابت بخادم الخلفية.
- ترقية `@capacitor/cli` إلى 8.5.0 وتثبيت مكوّنات Capacitor الإضافية.

### التحقق
- `npm run build` (خادم) ✅
- `npm run build:mobile` (ثابت/`out/`) ✅
- `tsc --noEmit` ✅ · `eslint` صفر أخطاء · `vitest` 9/9 ✅
